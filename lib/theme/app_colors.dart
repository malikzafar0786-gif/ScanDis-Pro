import 'package:flutter/material.dart';

/// Design tokens for ScanDis's dark glassmorphism theme.
/// Background #0B131F, cyan accent #00F2FE — as specified in the project brief.
class AppColors {
  static const Color background = Color(0xFF0B131F);
  static const Color surface = Color(0xFF121D2C);
  static const Color surfaceGlass = Color(0x99182437); // translucent glass panel
  static const Color cyan = Color(0xFF00F2FE);
  static const Color cyanDim = Color(0xFF0BC7D6);
  static const Color textPrimary = Color(0xFFF2F7FA);
  static const Color textSecondary = Color(0xFF8FA3B8);
  static const Color glassBorder = Color(0x3300F2FE); // faint cyan border
}
